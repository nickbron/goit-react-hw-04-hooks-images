import styled from '@emotion/styled/macro';

export const Button = styled.button`
  border: none;
  background: rgb(0, 166, 243);
  color: #ffffff;
  border-radius: 15px;
  padding: 15px 35px;

  margin: 0 auto;
  display: block;
`;
